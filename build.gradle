apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    maven { url 'https://oss.sonatype.org/content/repositories/snapshots' }
    mavenCentral()
}

dependencies {
    testCompile 'org.mongodb:mongo-java-driver:2.12.4'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}